# IPFS aliases
# ============

# Test Lab
# --------

grafana_pf() {
    pod=$(kubectl get pods --namespace=monitoring | grep grafana-core | awk '{print $1}')
    kubectl port-forward --namespace=monitoring $pod 3000:3000
}
