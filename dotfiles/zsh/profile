# command completion
autoload bashcompinit
bashcompinit
for comp in $DOTFILES/zsh/completion/*.bash; do source $comp; done

# Go path
export GOPATH="$HOME/.go"
# add GOPATH/bin to PATH
export PATH="$PATH:$GOPATH/bin"
